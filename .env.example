# Public (safe to expose in the browser bundle) — Supabase enforces
# authorization via Row Level Security, not by hiding these values.
NEXT_PUBLIC_SUPABASE_URL=https://gpojnwfacfqbguxrhdsd.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_Ba3gkWKClifICe-o6TgcTg_G6mb6D9K

# Server-only. NEVER prefix this with NEXT_PUBLIC_. Only needed for
# privileged server-side operations (none are required by this app
# today — every feature works under RLS with the publishable key).
# SUPABASE_SECRET_KEY=
